name: First Workflow
on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "30 14 * * *"

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo ${{ toJSON(github) }}
      - name: Print goodbye
        run: echo "Done - bye!"
